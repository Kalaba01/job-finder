<!DOCTYPE html>
<html lang="<%= locale %>">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= __("CandidateProfile.title") %></title>
    <link rel="stylesheet" href="/styles/candidate-profile.css" />
    <link rel="icon" type="image/png" href="/images/logo.png" />
  </head>
  <body>
    <%- include('../partials/top-bar') %>

    <div class="profile-container">
      <div class="profile-header">
        <div class="profile-image-container">
          <% if (candidate.profile_picture_id) { %>
          <img
            src="/images/<%= candidate.profile_picture_id %>"
            alt="<%= __('CandidateProfile.profilePictureAlt') %>"
            class="profile-picture"
          />
          <% } else { %>
          <div class="profile-placeholder">
            <%= __("CandidateProfile.noImage") %>
          </div>
          <% } %>
        </div>
        <div class="profile-name">
          <h1><%= candidate.first_name %> <%= candidate.last_name %></h1>
        </div>
      </div>

      <div class="profile-details">
        <div class="profile-detail-item">
          <h3><%= __("CandidateProfile.about") %></h3>
          <p><%= candidate.about %></p>
        </div>
        <div class="profile-detail-item">
          <h3><%= __("CandidateProfile.cv") %></h3>
          <% if (candidate.cv) { %>
            <a href="/files/cv/<%= candidate.id %>" target="_blank">Download CV</a>
          <% } else { %>
            <p>N/A</p>
          <% } %>
        </div>
        <div class="profile-detail-item">
          <h3><%= __("CandidateProfile.motivationLetter") %></h3>
          <% if (candidate.motivation_letter) { %>
            <a href="/files/motivation_letter/<%= candidate.id %>" target="_blank">Download Motivation Letter</a>
          <% } else { %>
            <p>N/A</p>
          <% } %>
        </div>
        <div class="profile-detail-item">
          <h3><%= __("CandidateProfile.recommendations") %></h3>
          <% if (candidate.recommendations) { %>
            <a href="/files/recommendations/<%= candidate.id %>" target="_blank">Download Recommendations</a>
          <% } else { %>
            <p>N/A</p>
          <% } %>
        </div>                
      </div>

      <button class="edit-profile-btn">
        <%= __("CandidateProfile.editProfile") %>
      </button>
    </div>
  </body>
</html>
